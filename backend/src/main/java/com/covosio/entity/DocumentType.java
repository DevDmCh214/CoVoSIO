package com.covosio.entity;

public enum DocumentType {
    /** Driver's license — not linked to a specific car. */
    LICENSE,
    /** Vehicle registration document — linked to a specific car. */
    CAR_REGISTRATION
}
